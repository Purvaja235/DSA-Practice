Check number is spy number

1.A =>  Most Optimized Approach
          int a = 1124;
	     int sum = 0;
	     int product = 1;
	     while(a > 0){
	         int digit = a % 10;
	         sum = sum + digit;
	         product = product * digit;
	         a = a / 10;
	     }
	     if(sum == product){
	     System.out.println("Spy Number");
	     }
	     else {
	         System.out.println("Not");
	     }
		 Time Complexity => O(log n)
        Space Complexity => O(1)
		
2.A =>  Better Approach
        There is no better algorithm.
        You must visit every digit at least once.
        The above solution is already optimal.	

3.A =>  Same solution		